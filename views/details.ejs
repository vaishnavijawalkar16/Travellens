<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Travel Lens - <%= landmarkName %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body>
  <header class="details-header">
    <i class="fa-solid fa-arrow-left" onclick="history.back()"></i>
  </header>

  <main class="details-container">
    <img class="details-img" src="<%= imageUrl || '/images/default-landmark.jpg' %>" alt="<%= landmarkName %>">
    <h2>
      <% if (wikipediaLink) { %>
        <a href="<%= wikipediaLink %>" target="_blank" rel="noopener"><%= landmarkName %></a>
      <% } else { %>
        <%= landmarkName %>
      <% } %>
    </h2>

    <p class="description"><%= description || "No description available." %></p>

    <div class="details-actions">
      <form action="/user/bookmark" method="POST" class="bookmark-form">
        <input type="hidden" name="landmarkName" value="<%= landmarkName %>">
        <input type="hidden" name="imageUrl" value="<%= imageUrl %>">
        <input type="hidden" name="wikiLink" value="<%= wikipediaLink %>">
        <input type="hidden" name="location" value="<%= location %>">
        <input type="hidden" name="description" value="<%= description %>">
        <button type="submit" class="btn-bookmark"><i class="fa-solid fa-bookmark"></i> Bookmark</button>
      </form>

      <button class="btn-share" onclick="navigator.share && navigator.share({ title: '<%= landmarkName %>', url: window.location.href })">
        <i class="fa-solid fa-share-nodes"></i> Share
      </button>
    </div>
  </main>

  <nav class="bottom-nav">
    <i class="fa-solid fa-house" onclick="location.href='/home'"></i>
    <i class="fa-solid fa-heart" onclick="location.href='/user/bookmarks'"></i>
    <i class="fa-solid fa-user" onclick="location.href='/account'"></i>
  </nav>
</body>
</html>
